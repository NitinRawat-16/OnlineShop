@model  UserInterface.ViewModel.ProductViewModel
@{
    ViewBag.Title = "ViewProducts";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<link href="~/Content/ProductSheet.css" rel="stylesheet" />
<h2>Products</h2>
<hr />

@foreach (var items in Model)
{
    <div class="box">

        <div class="col-md-3 thumbnail display">
            <img src="@Url.Content(items.products.productImage)" alt="Waterfall" />

            <div id="item">
                <div class="caption Name">
                    <h4> <span class="caption Name"> @items.products.ProductName </span></h4>
                </div>

                <div class="caption">
                    <span>@items.products.ProductDescription </span>
                </div>
                <div class="caption">
                    <span class="btn-danger">Rs. @items.products.ProductPrice/-</span>
                </div>
            </div>
            <div>
                <a href="@Url.Action("BuyNow", "User", items)" class="btn btn-lg btn-block btn-warning button">Buy Now</a>
                <a class="btn btn-lg btn-block button btn-orange">Add To Cart </a>
                <a href="@Url.Action("AddToWishlist", "User", items)" class="btn btn-lg btn-block button btn-warning">Wishlist</a>
            </div>
        </div>
    </div>
}


